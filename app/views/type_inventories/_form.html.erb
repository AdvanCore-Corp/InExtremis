<%= form_with(model: type_inventory) do |form| %>
  <% if type_inventory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_inventory.errors.count, "error") %> prohibited this type_inventory from being saved:</h2>

      <ul>
        <% type_inventory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :gym_id, style: "display: block" %>
    <%= form.text_field :gym_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
